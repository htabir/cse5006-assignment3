// Thrown by routes/helpers; rendered to a JSON response by middleware/errorHandler.ts.
export class HttpError extends Error {
  constructor(
    public readonly status: number,
    message: string,
  ) {
    super(message);
    this.name = 'HttpError';
  }
}
